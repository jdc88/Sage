# Core API
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.0.0

# Google ADK + Cloud (optional — install when deploying to GCP)
# google-adk>=1.0.0
# google-cloud-dlp>=3.0.0
# google-cloud-secret-manager>=2.0.0
