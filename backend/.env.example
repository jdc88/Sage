# Copy to .env and fill in locally — never commit .env

# Optional: enable live Google ADK + Gemini (Vertex AI)
# GEMINI_API_KEY=your-key-from-secret-manager

# Optional: Cloud DLP project for production PHI scrubbing
# GCP_PROJECT_ID=your-gcp-project-id

# CORS — add your Vercel URL when frontend is deployed
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
